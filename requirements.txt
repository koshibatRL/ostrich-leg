# Bipedal Leg Platform - Python Dependencies
# Install inside Docker container only

# Core
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0

# MuJoCo (CPU + GPU/MJX)
mujoco>=3.0.0
mujoco-mjx>=3.0.0

# JAX with CUDA (for MJX GPU acceleration)
jax[cuda12]>=0.4.20

# Neural network / optimization
flax>=0.8.0
optax>=0.1.7
distrax>=0.1.5

# Logging and visualization
tensorboard>=2.14.0
matplotlib>=3.7.0

# Video
imageio>=2.31.0
imageio-ffmpeg>=0.4.8

# Fallback (SB3, CPU-based)
gymnasium>=1.0.0
stable-baselines3>=2.3.0
torch>=2.1.0
